[package]
name = "aikit-evals"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Evaluation runner infrastructure for aikit agents"
repository = "https://github.com/goaikit/aikit"
keywords = ["eval", "agent", "ai", "testing"]
categories = ["development-tools::testing"]

[dependencies]
aikit-sdk = { path = "../aikit-sdk", version = "0.3.0" }
tokio = { version = "1", features = ["rt-multi-thread", "fs", "macros", "process", "sync"] }
async-trait = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "1.1"
thiserror = "2.0"
base64 = "0.23"
num_cpus = "1.16"
tracing = "0.1"
tempfile = "3"
similar = "2"
sha2 = "0.10"
chrono = "0.4"

[dev-dependencies]
mockito = "1.2"
jsonschema = "0.52"
